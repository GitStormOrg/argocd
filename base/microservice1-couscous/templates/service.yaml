apiVersion: v1
kind: Service
metadata:
  name: {{ include "node-express.fullname" . }}
  labels:
    app: {{ include "node-express.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  selector:
    app: {{ include "node-express.name" . }}
